{% extends 'layouts/front.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<h2>All Posts :</h2>
<div class="dropdown-divider mt-3 mb-5"></div>

{# <img src="{{ asset('build/image/1.jpg') }}"> #}

{% for post in posts %}

<h3 class="mb-3"><a href="{{ path('detailed-post', {'id' : post.id}) }}">{{ post.title }}<a></h3>

{% endfor %}

{% endblock %}
